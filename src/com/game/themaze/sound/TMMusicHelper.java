package com.game.themaze.sound;

import com.game.loblib.sound.MusicHelper;

public class TMMusicHelper extends MusicHelper {

	@Override
	public int getResourceId(int sound) {
		switch (sound) {
		default:
			return super.getResourceId(sound);
		}
	}
}
